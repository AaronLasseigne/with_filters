<%= with_filters_label(filter) %>
<% filter.choices.each do |choice| %>
  <%= label_tag("#{filter.field_name}[#{choice.value}]", choice.label) %>
  <%= radio_button_tag(filter.field_name, choice.value, choice.selected?, choice.attrs) %>
<% end %>
