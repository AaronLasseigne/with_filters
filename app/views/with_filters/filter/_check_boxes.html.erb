<%= with_filters_label(filter) %>
<% filter.collection.each do |choice| %>
  <%= label_tag("#{filter.field_name}[#{choice.value}]", choice.label) %>
  <%= check_box_tag(filter.field_name, choice.value, choice.selected?, choice.attrs) %>
<% end %>
